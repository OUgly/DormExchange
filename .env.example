NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# SMTP settings for Contact page (required to send email)
# These are example placeholders — use your provider’s values
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
# Set to true for port 465 (SSL), otherwise false
SMTP_SECURE=false

# Optional overrides
# CONTACT_FROM should be a verified/sender email in your SMTP provider
CONTACT_FROM=
CONTACT_TO=dormxchangeteam@gmail.com
